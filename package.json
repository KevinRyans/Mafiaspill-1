{
  "name": "mafiaspill-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "docker compose up -d db redis && concurrently -n shared,api,web -c magenta,blue,green \"npm --prefix packages/shared run dev\" \"npm --prefix apps/api run dev\" \"npm --prefix apps/web run dev\"",
    "dev:apps": "concurrently -n shared,api,web -c magenta,blue,green \"npm --prefix packages/shared run dev\" \"npm --prefix apps/api run dev\" \"npm --prefix apps/web run dev\"",
    "build": "npm --prefix packages/shared run build && npm --prefix apps/api run build && npm --prefix apps/web run build",
    "lint": "npm --prefix apps/api run lint && npm --prefix apps/web run lint",
    "test": "npm --prefix packages/shared run build && npm --prefix apps/api run test && npm --prefix apps/web run test",
    "db:generate": "npm --prefix apps/api run db:generate",
    "db:migrate": "npm --prefix apps/api run db:migrate",
    "db:seed": "npm --prefix apps/api run db:seed",
    "db:studio": "npm --prefix apps/api run db:studio"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
